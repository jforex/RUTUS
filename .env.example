# Shared
CROO_API_URL="https://api.croo.network"
CROO_WS_URL="wss://api.croo.network/ws"

# Provider agent (the Echo agent that receives orders)
PROVIDER_SDK_KEY="croo_sk_..."

# Requester agent (the agent that calls Echo)
REQUESTER_SDK_KEY="croo_sk_..."

# The Echo service's ID (from the dashboard, after you configure the service)
TARGET_SERVICE_ID="..."
